node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
*.log
.env
.env.local
*.tsbuildinfo
# Runtime data — never commit
data/

# Hardhat build output
contracts/artifacts-hh/
contracts/cache-hh/
# solc deploy-script artifacts (regenerated by the deploy scripts)
contracts/artifacts/

# Agent / skills tooling — not part of the project
.agents/
.claude/
skills-lock.json

# Internal docs / plans — kept local, not exposed on GitHub. Only the root
# README.md is the public face. `src/config/docsContent.md` is source code
# (imported via Vite ?raw) so it is intentionally not under docs/ and stays
# tracked.
docs/
TEST-SCENARIOS.md
TESTING.md
server/README.md

# Ad-hoc smoke / diagnostic scripts (created during sessions, then deleted)
smoke-*.tmp.mjs
*.tmp.mjs
